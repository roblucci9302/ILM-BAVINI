/**
 * Template Loading Service
 * Exports functions for loading pre-built template files
 */

export { loadTemplate, getTemplateFiles, getTemplateMetadata, listAvailableTemplates } from './template-loader';
export type { TemplateFile, TemplateMetadata, TemplateFilesResponse, TemplateErrorResponse } from './types';
